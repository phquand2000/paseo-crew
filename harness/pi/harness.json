{
  "id": "pi",
  "label": "Pi",
  "baseProvider": "pi",
  "verified": "0.85.1",
  "profileRoot": "HOME/.pi/profiles",
  "configDirEnv": "PI_CODING_AGENT_DIR",
  "promptFile": "APPEND_SYSTEM.md",
  "skillsDir": "skills",
  "promptComments": "shown",
  "hasModes": false,
  "settings": {
    "mode": "merge",
    "file": "settings.json",
    "source": "settings.json"
  },
  "links": [
    {
      "link": "auth.json",
      "target": "HOME/.pi/agent/auth.json",
      "required": "log in with `pi` (/login) or save an API key first"
    }
  ],
  "state": {
    "file": "mcp.json",
    "optional": true,
    "checkMcp": "((.mcpServers // {}) | has(\"paseo\")) | not"
  },
  "guards": {
    "dir": "pi/extensions",
    "installTo": "extensions",
    "hookProtocol": "extension",
    "readOnlyEnv": "SEATWORKS_READ_ONLY",
    "dirNote": "relative to harness/; the shell guards are protocol-generic and shared, so every hook-based harness points at common/guards"
  },
  "deny": {
    "mechanism": "extension",
    "paseoToolsOff": true,
    "intents": {},
    "enforcedByGuard": [
      "git-push",
      "forge-cli",
      "agent-cli",
      "file-edit"
    ],
    "note": "Pi ignores disallowedTools, so nothing maps to a tool name. peer-guard.ts blocks git push, gh and agent CLIs for every Pi seat, and the file tools when SEATWORKS_READ_ONLY=1. The intents not listed here are not enforced on this harness: a Pi seat can still use its own web search, plan mode or todo list."
  },
  "skillLoad": {
    "tool": "read",
    "phrase": "a read of its SKILL.md",
    "force": "/skill:NAME",
    "transcriptMatch": null,
    "descriptionBudget": null,
    "phraseNote": "a noun phrase, because guards say \"Load it with <phrase>\""
  },
  "sharedSkillDirs": [
    "HOME/.agents/skills"
  ],
  "probe": {
    "kind": "pi-loader",
    "note": "node, calling Pi's own loadSkills() with agentDir set to the seat profile; authoritative and free, so it runs on every --check"
  },
  "versionCommand": "pi --version",
  "provider": {
    "env": {
      "PI_SKIP_VERSION_CHECK": "1",
      "PI_TELEMETRY": "0"
    },
    "keys": {
      "paseoTools": {
        "enabled": false
      }
    },
    "profileModeId": null,
    "defaultModel": "zai/glm-5.3",
    "defaultModelNote": "the model a role on this harness gets when seats.json names none and --model is not passed. Spelled the way this harness spells a model: Pi wants provider/id, Codex wants the bare slug with model_provider set separately.",
    "baseCredential": null,
    "baseCredentialNote": "null: this harness authenticates through the login file in links, not through a token on its Paseo base provider."
  },
  "contextFile": "AGENTS.md",
  "contextFileNeedsPointer": false,
  "promptCommentsNote": "informational. Every .md in this kit is comment-free so it loads unchanged on every harness; setup refuses `<!--` whatever this says. A safety review reads it to know whether a leaked note would reach the seat."
}
