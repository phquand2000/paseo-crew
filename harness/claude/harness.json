{
  "id": "claude",
  "label": "Claude Code",
  "baseProvider": "claude",
  "verified": "2.1.236",
  "profileRoot": "HOME/.claude/profiles",
  "configDirEnv": "CLAUDE_CONFIG_DIR",
  "promptFile": "CLAUDE.md",
  "skillsDir": "skills",
  "promptComments": "stripped",
  "hasModes": true,
  "settings": {
    "mode": "link",
    "file": "settings.json",
    "source": "settings/ROLE.settings.json",
    "retentionKey": "cleanupPeriodDays",
    "valueNote": "autoCompactWindow is 333000 so compaction lands at 300000, because this harness offsets it: read from the 2.1.236 binary, the headless runner compacts when tokens exceed Jir(model, autoCompactWindow), which is min(modelWindow, autoCompactWindow) minus min(model max output, 20000) minus 13000. claude-opus-5 reports a 1M window and a 64000 output default, so the offset is a flat 33000. Two consequences worth knowing before changing it: the setting can only shrink the window, never grow it, and a CLAUDE_CODE_MAX_OUTPUT_TOKENS under 20000 in a provider env would raise the real point. omp spells the same idea as a literal threshold instead, so its numbers need no offset."
  },
  "links": [
    {
      "link": "projects",
      "target": "HOME/.claude/projects"
    }
  ],
  "state": {
    "file": ".claude.json",
    "seed": "{\"hasCompletedOnboarding\": true}",
    "clearMcp": ".mcpServers = $servers | .enabledMcpjsonServers = [] | del(.enableAllProjectMcpServers) | .projects = ((.projects // {}) | with_entries(.value.mcpServers = {}))",
    "checkMcp": "(.mcpServers // {}) == $servers and ((.enabledMcpjsonServers // []) | length) == 0",
    "forbid": [
      ".credentials.json"
    ],
    "mcpNote": "$servers is seats.json's mcpServers map, passed in by setup-seats.fish. The seat gets those at user scope in its own .claude.json and nothing else: project-scoped servers stay cleared, because a repository could otherwise hand a seat a server the kit never chose."
  },
  "guards": {
    "dir": "common/guards",
    "installTo": ".",
    "hookProtocol": "exit-code",
    "hookSettingsKey": "hooks",
    "noteProtocol": "additionalContext",
    "noteProtocolNote": "how a script tells the model something without refusing the call: hookSpecificOutput.additionalContext, which note in harness/common/hook-io.sh writes. Measured on 2.1.236 from PostToolUse and PreToolUse; NOTES.md says how. A role whose guards include a note-only script such as record-check.sh belongs on a harness that sets this field.",
    "dirNote": "relative to harness/; the shell guards are protocol-generic and shared, so every hook-based harness points at common/guards",
    "shellTool": "Bash"
  },
  "deny": {
    "mechanism": "disallowedTools",
    "intents": {
      "subagents": [
        "Agent",
        "Task"
      ],
      "workflows": [
        "Workflow"
      ],
      "web-search": [
        "WebSearch"
      ],
      "todo-list": [
        "TodoWrite"
      ],
      "plan-mode": [
        "EnterPlanMode",
        "ExitPlanMode"
      ],
      "agent-cli": [
        "Bash(claude:*)",
        "Bash(npx claude:*)"
      ],
      "git-push": [
        "Bash(git push:*)"
      ],
      "forge-cli": [
        "Bash(gh:*)"
      ],
      "lsp": [
        "LSP"
      ],
      "file-edit": [
        "Edit",
        "Write",
        "MultiEdit",
        "NotebookEdit"
      ],
      "paseo-write": [
        "mcp__paseo__archive_agent",
        "mcp__paseo__archive_workspace",
        "mcp__paseo__cancel_agent",
        "mcp__paseo__create_agent",
        "mcp__paseo__create_schedule",
        "mcp__paseo__create_terminal",
        "mcp__paseo__create_workspace",
        "mcp__paseo__delete_schedule",
        "mcp__paseo__kill_agent",
        "mcp__paseo__kill_terminal",
        "mcp__paseo__pause_schedule",
        "mcp__paseo__rename_workspace",
        "mcp__paseo__respond_to_permission",
        "mcp__paseo__resume_schedule",
        "mcp__paseo__run_schedule_once",
        "mcp__paseo__send_terminal_keys",
        "mcp__paseo__set_agent_mode",
        "mcp__paseo__speak",
        "mcp__paseo__start_workspace_script",
        "mcp__paseo__stop_workspace_script",
        "mcp__paseo__update_agent",
        "mcp__paseo__update_schedule"
      ],
      "ide-refactor": [
        "mcp__intellij-index__ide_refactor_rename",
        "mcp__intellij-index__ide_move_file",
        "mcp__intellij-index__ide_refactor_safe_delete"
      ],
      "workspaces": [
        "mcp__paseo__create_workspace",
        "mcp__paseo__archive_workspace",
        "mcp__paseo__rename_workspace"
      ],
      "artifact-publish": [
        "Artifact"
      ],
      "local-worktree": [
        "EnterWorktree",
        "ExitWorktree"
      ],
      "cron": [
        "CronCreate",
        "CronDelete",
        "CronList"
      ],
      "agent-messaging": [
        "SendMessage",
        "ListAgents"
      ],
      "design-sync": [
        "DesignSync"
      ]
    },
    "enforcedByGuard": [],
    "retired": [
      "SlashCommand"
    ],
    "retiredNote": "Claude Code 2.1.236 answers a SlashCommand deny rule with `matches no known tool — check for typos`. Skills load through the Skill tool, which must stay allowed.",
    "absent": [
      "code-eval"
    ],
    "absentNote": "intents this harness has no capability for, so there is nothing to deny and nothing to report. Claude Code runs code only through Bash, which the guards already read.",
    "mcpNamesNote": "Claude Code registers mcp__<server>__<tool> with the server name as written in the config, so these keep the hyphen. This is the documented spelling and the one name in this manifest not read off a running seat; a wrong one answers `matches no known tool`, the way deny.retired records.",
    "measuredNote": "the tool names above were read off a captured request, not guessed: a lead seat launched the way the orchestrator launches one (--output-format stream-json --input-format stream-json), with this harness provider.env, its role settings file and the deny list seats.json computes, was offered 26 tools, and Artifact, EnterWorktree, ExitWorktree, CronCreate, CronDelete, CronList, SendMessage, ListAgents and DesignSync were among them; denying those leaves 17. A -p measurement undercounts by three, because AskUserQuestion, EnterPlanMode and ExitPlanMode are offered only in the streaming shape. provider.env sets CLAUDE_CODE_DISABLE_CRON, and the Cron tools were offered anyway, so the variable does not stand in for a deny."
  },
  "skillLoad": {
    "tool": "Skill",
    "phrase": "the Skill tool",
    "force": "/NAME",
    "transcriptMatch": "\"name\":\"Skill\",\"input\":{\"skill\":\"NAME\"",
    "descriptionBudget": 8000,
    "phraseNote": "a noun phrase, because guards say \"Load it with <phrase>\"",
    "placeholders": [
      "$ARGUMENTS",
      "${CLAUDE_SKILL_DIR}"
    ],
    "placeholdersNote": "substitutions this harness performs. setup-seats.fish refuses every harness's placeholders in every skill, because a skill has to load unchanged where none of them are substituted."
  },
  "probe": {
    "kind": "version",
    "command": [
      "claude",
      "-p",
      "--model",
      "claude-haiku-4-5"
    ],
    "credentialFromBase": true,
    "live": "run probe.command with the seat's configDirEnv set and ask it to list its skills",
    "note": "the skills directory and symlink following were verified by running a seat against a scratch profile; a version other than the verified one re-opens that question"
  },
  "versionCommand": "claude --version",
  "provider": {
    "env": {
      "CLAUDE_CODE_DISABLE_BUNDLED_SKILLS": "1",
      "CLAUDE_CODE_DISABLE_CRON": "1",
      "CLAUDE_CODE_DISABLE_FAST_MODE": "1",
      "CLAUDE_CODE_DISABLE_GIT_INSTRUCTIONS": "1",
      "CLAUDE_CODE_DISABLE_WORKFLOWS": "1",
      "CLAUDE_CODE_SIMPLE_SYSTEM_PROMPT": "1",
      "SEATWORKS_HARNESS": "claude",
      "SEATWORKS_AGENT_BIN": "claude"
    },
    "profileModeId": "bypassPermissions",
    "defaultModel": null,
    "defaultModelNote": "the model a role on this harness gets when seats.json names none and --model is not passed. Spelled the way this harness spells a model: omp wants provider/id.",
    "baseCredential": {
      "env": "CLAUDE_CODE_OAUTH_TOKEN",
      "create": "claude setup-token",
      "note": "set on the base provider so every seat inherits it through extends. The kit never reads it from anywhere but the user."
    },
    "command": [
      "KIT/harness/common/bin/seat-room"
    ],
    "commandNote": "every harness launches through the shared room, which resolves the project from the agent's working directory, points configDirEnv at that project's seat directory, and execs the binary in provider.env.SEATWORKS_AGENT_BIN with every argument untouched",
    "forceFlags": {
      "--setting-sources": "user"
    },
    "forceFlagsNote": "flags the room replaces in the argv the orchestrator built, or appends when absent, because the kit writes the provider but not its arguments. --setting-sources user drops the repository own .claude/settings.json and .claude/skills/ while keeping the seat own settings.json, which is the user source: measured from this harness debug log, user,project,local loads 2 skills (user: 1, project: 1) and user loads 1 (user: 1, project: 0), and the watch list names the seat file first. Without it a repository adds skills and settings to a seat that no hidesWords check and no skill set ever saw."
  },
  "contextFile": "CLAUDE.md",
  "contextFileNeedsPointer": true,
  "promptCommentsNote": "informational. Every .md in this kit is comment-free so it loads unchanged on every harness; setup refuses `<!--` whatever this says. A safety review reads it to know whether a leaked note would reach the seat.",
  "userSettings": "HOME/.claude/settings.json",
  "userSettingsNote": "the user's own settings file. setup-seats.fish requires each role settings file to carry the same settings.retentionKey value, because a seat prunes the shared directories in links on startup.",
  "retiredLinks": [
    "plugins"
  ],
  "retiredLinksNote": "links this harness once made and must not have now. setup deletes each one from every seat directory, because build_seat_links only creates what links declares and would otherwise leave a retired symlink in place. plugins is retired because no seat uses a plugin: the Human asked for none, both LSP plugins were enabled on seats whose lsp intent denies the LSP tool, they ship no skills, and installed_plugins.json recorded their install path inside a profile directory the kit had stopped creating. Without the link a seat has no plugin directory at all, so nothing can be enabled by default either."
}
