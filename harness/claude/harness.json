{
  "id": "claude",
  "label": "Claude Code",
  "baseProvider": "claude",
  "verified": "2.1.236",
  "profileRoot": "HOME/.claude/profiles",
  "configDirEnv": "CLAUDE_CONFIG_DIR",
  "promptFile": "CLAUDE.md",
  "skillsDir": "skills",
  "promptComments": "stripped",
  "hasModes": true,
  "settings": {
    "mode": "link",
    "file": "settings.json",
    "source": "settings/ROLE.settings.json",
    "retentionKey": "cleanupPeriodDays"
  },
  "links": [
    {
      "link": "projects",
      "target": "HOME/.claude/projects"
    },
    {
      "link": "plugins",
      "target": "HOME/.claude/plugins"
    }
  ],
  "state": {
    "file": ".claude.json",
    "seed": "{\"hasCompletedOnboarding\": true}",
    "clearMcp": ".mcpServers = {} | .enabledMcpjsonServers = [] | del(.enableAllProjectMcpServers) | .projects = ((.projects // {}) | with_entries(.value.mcpServers = {}))",
    "checkMcp": "(.mcpServers // {}) == {} and ((.enabledMcpjsonServers // []) | length) == 0",
    "forbid": [
      ".credentials.json"
    ]
  },
  "guards": {
    "dir": "common/guards",
    "installTo": ".",
    "hookProtocol": "exit-code",
    "hookSettingsKey": "hooks.PreToolUse",
    "dirNote": "relative to harness/; the shell guards are protocol-generic and shared, so every hook-based harness points at common/guards",
    "shellTool": "Bash"
  },
  "deny": {
    "mechanism": "disallowedTools",
    "intents": {
      "subagents": [
        "Agent",
        "Task"
      ],
      "workflows": [
        "Workflow"
      ],
      "web-search": [
        "WebSearch"
      ],
      "todo-list": [
        "TodoWrite"
      ],
      "plan-mode": [
        "EnterPlanMode",
        "ExitPlanMode"
      ],
      "agent-cli": [
        "Bash(claude:*)",
        "Bash(npx claude:*)"
      ],
      "git-push": [
        "Bash(git push:*)"
      ],
      "forge-cli": [
        "Bash(gh:*)"
      ],
      "lsp": [
        "LSP"
      ],
      "file-edit": [
        "Edit",
        "Write",
        "MultiEdit",
        "NotebookEdit"
      ]
    },
    "enforcedByGuard": [],
    "retired": [
      "SlashCommand"
    ],
    "retiredNote": "Claude Code 2.1.236 answers a SlashCommand deny rule with `matches no known tool — check for typos`. Skills load through the Skill tool, which must stay allowed."
  },
  "skillLoad": {
    "tool": "Skill",
    "phrase": "the Skill tool",
    "force": "/NAME",
    "transcriptMatch": "\"name\":\"Skill\",\"input\":{\"skill\":\"NAME\"",
    "descriptionBudget": 8000,
    "phraseNote": "a noun phrase, because guards say \"Load it with <phrase>\"",
    "placeholders": [
      "$ARGUMENTS",
      "${CLAUDE_SKILL_DIR}"
    ],
    "placeholdersNote": "substitutions this harness performs. setup-seats.fish refuses every harness's placeholders in every skill, because a skill has to load unchanged where none of them are substituted."
  },
  "probe": {
    "kind": "version",
    "command": [
      "claude",
      "-p",
      "--model",
      "claude-haiku-4-5"
    ],
    "credentialFromBase": true,
    "live": "run probe.command with the seat's configDirEnv set and ask it to list its skills",
    "note": "the skills directory and symlink following were verified by running a seat against a scratch profile; a version other than the verified one re-opens that question"
  },
  "versionCommand": "claude --version",
  "provider": {
    "env": {
      "CLAUDE_CODE_DISABLE_BUNDLED_SKILLS": "1",
      "CLAUDE_CODE_DISABLE_CRON": "1",
      "CLAUDE_CODE_DISABLE_FAST_MODE": "1",
      "CLAUDE_CODE_DISABLE_GIT_INSTRUCTIONS": "1",
      "CLAUDE_CODE_DISABLE_WORKFLOWS": "1",
      "CLAUDE_CODE_SIMPLE_SYSTEM_PROMPT": "1",
      "SEATWORKS_HARNESS": "claude",
      "SEATWORKS_AGENT_BIN": "claude"
    },
    "keys": {
      "disallowedTools": []
    },
    "profileModeId": "bypassPermissions",
    "defaultModel": null,
    "defaultModelNote": "the model a role on this harness gets when seats.json names none and --model is not passed. Spelled the way this harness spells a model: Pi wants provider/id, Codex wants the bare slug with model_provider set separately.",
    "baseCredential": {
      "env": "CLAUDE_CODE_OAUTH_TOKEN",
      "create": "claude setup-token",
      "note": "set on the base provider so every seat inherits it through extends. The kit never reads it from anywhere but the user."
    },
    "command": [
      "KIT/harness/common/bin/seat-room"
    ],
    "commandNote": "every harness launches through the shared room, which resolves the project from the agent's working directory, points configDirEnv at that project's seat directory, and execs the binary in provider.env.SEATWORKS_AGENT_BIN with every argument untouched"
  },
  "contextFile": "CLAUDE.md",
  "contextFileNeedsPointer": true,
  "promptCommentsNote": "informational. Every .md in this kit is comment-free so it loads unchanged on every harness; setup refuses `<!--` whatever this says. A safety review reads it to know whether a leaked note would reach the seat.",
  "userSettings": "HOME/.claude/settings.json",
  "userSettingsNote": "the user's own settings file. setup-seats.fish requires each role settings file to carry the same settings.retentionKey value, because a seat prunes the shared directories in links on startup.",
  "pluginIndex": "HOME/.claude/plugins/installed_plugins.json",
  "pluginManifest": ".claude-plugin/plugin.json",
  "pluginManifestNote": "path inside an installed plugin whose `skills` array lists the skill directories it ships. Read together with pluginIndex."
}
