---
name: constitution
owns: the short list of things this project will not do, and what reads it
prevents: quiet drift to the least painful patch, one reasonable-looking exception at a time
activate: the project has a standing policy an agent would otherwise break
ceremony: it restates what the language or the tools already do, or nothing reads it
---

A rule nothing reads is decoration. Name what enforces each one, and delete any rule where that line is empty.

# Project policy

Numbered, short, and each one a thing somebody would otherwise do. A rule nobody would break
without it is not a rule, it is padding.

Not every heading applies. Where one does not, say in a line why, and leave it. An empty heading is ceremony; a heading that says why it is empty is information.

1. <rule>
   Why: <what goes wrong without it>
   Read by: <which template, gate or check actually enforces this>

## Changing this

<what happens when a rule changes: which other pages have to be re-read, and who decides.>
