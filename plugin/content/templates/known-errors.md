---
name: known-errors
owns: a confirmed recurring defect in this project, its cause, and the way round it
prevents: the next agent, holding none of the last one's context, committing the same defect again
activate: only once the same defect has recurred HERE
ceremony: it duplicates the shared anti-pattern list — link to that instead
---

# Known errors

Only what has gone wrong in **this** project more than once. Anything general enough to apply
elsewhere belongs in a skill or an instruction, not here; a per-project list of general advice is
over-optimising, and it goes stale where a shared one does not.

Not every heading applies. Where one does not, say in a line why, and leave it. An empty heading is ceremony; a heading that says why it is empty is information.

| What goes wrong | Why | The way round it | Fixed? |
|---|---|---|---|
